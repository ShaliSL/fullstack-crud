export interface Order {
    name: string;
    product: string;
    quantity: string;
    date: string;
    total: string;
 }

 export interface Product {
    name: string;
    price: string;
 }

